{
  "name": "ts-lint",
  "version": "1.0.0",
  "description": "Template with code formatters",
  "author": "Kevin-Leguiza-Gaggero",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "lint": "npx eslint --ignore-path .eslintignore --ext .js,.ts .",
    "lint:fix": "npx eslint --ignore-path .eslintignore --ext .js,.ts . --fix",
    "build": "npm run lint && npm run format && npx tsc",
    "format": "npx prettier --ignore-path .gitignore --write \"**/*.+(js|ts|json)\"",
    "prepare": "husky install"
  },
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^16.1.0",
    "@commitlint/config-conventional": "^16.0.0",
    "@types/node": "^17.0.16",
    "@typescript-eslint/eslint-plugin": "^5.11.0",
    "@typescript-eslint/parser": "^5.11.0",
    "eslint": "^8.8.0",
    "eslint-config-prettier": "^8.3.0",
    "husky": "^7.0.0",
    "lint-staged": "^12.3.3",
    "prettier": "^2.5.1",
    "ts-node": "^10.5.0",
    "typescript": "^4.5.5"
  },
  "pre-commit": "lint-staged",
  "lint-staged": {
    "**/*.ts": [
      "npx eslint --fix",
      "npx prettier --write"
    ]
  }
}
